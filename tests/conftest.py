# SPDX-License-Identifier: MIT
"""
Shared pytest fixtures for the full test suite.

Fixtures defined here are available to all tests in tests/unit/
and tests/integration/ without any explicit import.

Add shared fixtures here as the application grows — for example:
- A test database (SQLite in-memory)
- A pre-authenticated test client
- Mock environment variables

Implementation: Issue #36 (unit tests) and Issue #37 (integration tests).
"""
